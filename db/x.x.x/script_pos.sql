-- POS (x.x.x - mock)
-- Este arquivo deve conter tudo o que precisa ocorrer DEPOIS das tabelas principais.
-- Ex.: seeds, backfills, indices, views e ajustes de performance.

-- Exemplo:
-- CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_inbox_status_created
-- ON inbota.inbox_items (status, created_at DESC);
